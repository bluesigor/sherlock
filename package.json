{
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "cross-env SKIP_ENV_VALIDATION=1 yarn workspaces foreach -A --topological run build",
    "test": "yarn workspaces foreach -A run test --run",
    "dev": "yarn dev:services:up && yarn dev:zoekt:build && yarn dev:prisma:migrate:dev && npm-run-all --print-label --parallel dev:zoekt dev:backend dev:web",
    "with-env": "cross-env PATH=\"$PWD/bin:$PATH\" dotenv -e .env.development -c --",
    "dev:zoekt": "yarn with-env zoekt-webserver -index .sourcebot/index -rpc",
    "dev:backend": "yarn with-env yarn workspace @sourcebot/backend dev:watch",
    "dev:web": "yarn with-env yarn workspace @sourcebot/web dev",
    "dev:prisma:migrate:dev": "yarn with-env yarn workspace @sourcebot/db prisma:migrate:dev",
    "dev:prisma:studio": "yarn with-env yarn workspace @sourcebot/db prisma:studio",
    "dev:prisma:migrate:reset": "yarn with-env yarn workspace @sourcebot/db prisma:migrate:reset",
    "dev:db:up": "docker start sherlock-pg 2>/dev/null || docker run -d --name sherlock-pg -e POSTGRES_USER=postgres -e POSTGRES_PASSWORD=postgres -e POSTGRES_DB=postgres -p 127.0.0.1:5432:5432 postgres:16-alpine",
    "dev:redis:up": "docker start sherlock-redis 2>/dev/null || docker run -d --name sherlock-redis -p 127.0.0.1:6379:6379 redis:7.2-alpine",
    "dev:redis:flush": "docker exec sherlock-redis redis-cli FLUSHALL",
    "dev:services:up": "npm-run-all --parallel dev:db:up dev:redis:up",
    "dev:zoekt:build": "cd vendor/zoekt && GOTOOLCHAIN=go1.23.4+auto go build -o ../../bin/zoekt-git-index ./cmd/zoekt-git-index && GOTOOLCHAIN=go1.23.4+auto go build -o ../../bin/zoekt-webserver ./cmd/zoekt-webserver"
  },
  "devDependencies": {
    "cross-env": "^7.0.3",
    "dotenv-cli": "^8.0.0",
    "npm-run-all": "^4.1.5"
  },
  "packageManager": "yarn@4.7.0",
  "engines": {
    "node": ">=24.15.0"
  }
}
